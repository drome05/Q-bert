# Placeholder only -- never apply this file directly. Create the real
# secret imperatively instead:
#
#   kubectl create secret generic gateway-secrets -n gateway \
#     --from-literal=DISCORD_BOT_TOKEN=xxx \
#     --from-literal=GUILD_ID=xxx
#
apiVersion: v1
kind: Secret
metadata:
  name: gateway-secrets
  namespace: gateway
type: Opaque
stringData:
  DISCORD_BOT_TOKEN: "xxx"
  GUILD_ID: "xxx"
