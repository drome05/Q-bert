apiVersion: apps/v1
kind: Deployment
metadata:
  name: blackjack-service
  namespace: casino
spec:
  replicas: 1   # holds in-memory hand state; keep single-replica so sessions don't split across pods
  selector:
    matchLabels:
      app: blackjack-service
  template:
    metadata:
      labels:
        app: blackjack-service
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: tier
                    operator: In
                    values: ["games"]
      containers:
        - name: blackjack-service
          image: ghcr.io/drome05/blackjack-service:27ee8a1c2b23aae38c2229dd0db88777203ea412
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "150m"
              memory: "128Mi"
